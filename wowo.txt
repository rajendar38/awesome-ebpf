
keytool -genkeypair -alias server -keyalg RSA -keysize 2048 -storetype PKCS12 -keystore server.p12 -validity 365


Export the server's certificate from the keystore and import it into the client's truststore:

keytool -export -alias server -file server.crt -keystore server.p12
keytool -import -trustcacerts -alias server -file server.crt -keystore client-truststore.jks


keytool -genkeypair -alias server -keyalg RSA -keysize 2048 -storetype PKCS12 -keystore server.p12 -validity 365


keytool -list -keystore server.p12 -storetype PKCS12
